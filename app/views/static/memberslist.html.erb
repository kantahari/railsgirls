<% provide(:title, 'Members list') %>
<div class="hero-unit"><h3>Members List</h3></div>
<ul>
<% @members.each do |member| %>
<li>
  <div>
    <strong><%= member.username.capitalize %></strong>
    <br />
    <strong><%= member.email %></strong>
    <br/>
  </div>
  </li>
<% end %>
  </ul>
<%= will_paginate @members %>
